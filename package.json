{
  "name": "betangar-erp",
  "private": true,
  "description": "ERP Betangar — app estática (app.html + app.js) servida por GitHub Pages. Este package.json es solo para QA/tests locales; GitHub Pages lo ignora.",
  "scripts": {
    "check": "node --check money.js && node --check app.js",
    "test": "node --check money.js && node --check app.js && node test/test.js"
  }
}
