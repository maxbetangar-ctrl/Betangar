# ───────────────────────────────────────────────────────────
# RESPALDOS DE DATOS — NUNCA subir a GitHub.
# Este repositorio es PÚBLICO. La carpeta respaldos/ contiene
# datos sensibles (empleados, cédulas, cuentas bancarias, fotos,
# planillas, abonos). git la ignora para que jamás llegue a internet.
# ───────────────────────────────────────────────────────────
respaldos/
*.csv

# QA / tooling local (no afecta a GitHub Pages)
node_modules/
package-lock.json
.playwright-mcp/

# ⛔ El volcado COMPLETO de la base para el banco de auditoría de combustible. Este repo es
# PÚBLICO: la historia entera de un cliente que paga —con nombres de choferes— no va acá.
# Lo que sí se versiona es `pruebas/datos/combustible.json`, recortado y sin personas
# (lo arma `pruebas/datos/reducir.mjs`).
pruebas/datos/combustible-real.json
